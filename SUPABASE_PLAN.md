# Supabase 迁移方案与交接

## 2026-09-23 计算器修复

最新市场报告采用 `market-report` 编排 + `market-compute` 内部计算：全市/区汇总、小区排行、全市趋势、各区趋势分四份执行，合并后一次缓存和交付。内部接口仅接受 service role，检查计算 lease 和账号权益。分位数采用精确选择及滑动有序合并，年度分片逐个校验，算法缓存版本为 `market-edge-v5`。SQL 计算器保留为本地全量对账参考，生产请求不再上传大块公开历史 JSON 给 SQL。旧的“最新全市未实现”描述已失效。


## 2026-09-22 混合权限、市场报告与缓存更新

- 第三份迁移 `202609170003_admin_and_view_quotas.sql` 已部署。用户指定的已验证邮箱账号已按真实 UUID 授予应用超级管理员；不写入公共前端、不用可修改 user_metadata 判断权限。
- `admin.html` 提供邮箱绑定发码、天数、兑换期限、最多查看次数/不限、订单幂等；仅服务端管理员能调用。明文码仅本页内存保留，不写浏览器存储。尚无支付和完整订单/退款系统。
- `housing_view_community` 全小区新成交一次原子返回，有限码扣一次；管理员/已领取试用不扣。十分钟内同账号同小区请求重试不重复扣；旧分页接口拒绝有限码。已兑换码的有效期与次数共同限制；旧码 max_views=NULL 保持不限。
- 2026-09-22 已增加受控完整市场报告：管理员免码，付费账号成功查看一份市场报告扣 1 次；小区新成交查看同样扣 1 次并共用余额。报告由可信函数把固定版本公开历史与私有增量精确拼接，结果只进入私有缓存。
- 云端 `scripts/verify_admin.sql` 已通过：普通账号发码拒绝、管理员免码、绑定发码、幂等、兑换、限额扣减/用尽、重试不重复扣、旧接口防绕过；所有测试账号/测试码事务回滚。未签发真实客户兑换码。
- Safari 真实本地管理员地址已渲染并确认未登录只见拒绝/登录提示。另用完全离线模拟 RPC 页面验收桌面邮箱/200 次发码表单，以及 390px iframe 窄屏与不限次数开关；不等于真实登录发码或真手机触摸验收。该模拟服务仅绑定 loopback，脚本在 tests/ 下，不进入 docs/ 或本地生产白名单。

状态：项目 `mehbviiakjcbfckonzqk` 位于孟买 ap-south-1；**64,867 条新成交，2025-09-01 至 2026-08-29**，存于非公开 `housing_private` schema。账号权限、共享限次、私有市场缓存、计算 lease、报告 receipt 与服务端报告函数均已部署。未签发真实付费兑换码；售卖暂采用页面展示的微信人工联系与管理员绑定邮箱发码。

## 本轮已完成

- 官方 CLI 2.117.0 已认证，目标项目已 link；权限、目录、管理员限次码、市场缓存与市场计算迁移已部署，导入计数/日期与本机源数据一致。导入 SQL 仅使用本机忽略的 `.private/` 临时文件，不输出逐笔数据。
- `202609170001_account_entitlements.sql`：非公开新成交表、账号绑定两个名额、已验证邮箱校验、同账号事务锁、一次兑换码、兑换起算、撤销、分页上限与兑换限速。`202609170002_catalog_and_read_limits.sql`：匿名可搜名称目录、受保护读请求每账号每分钟 120 次。
- Resend SMTP 已确认启用（smtp.resend.com:465，发件人 no-reply@auth.liyongzhi.xyz）；保留现有秘密配置，发送限额 30 封/小时，同邮箱间隔 60 秒。OTP 为 8 位、10 分钟有效；确认邮件与 magic-link 模板均改为显示验证码。官方 CLI 当前版本保留未声明配置，**仍必须先审查 config diff 再 push，不能盲目覆盖 SMTP/MFA 等设置**。
- Site URL 为 `https://liyongzhi.xyz/beijing-housing-dashboard/`，本地回跳允许 `http://127.0.0.1:18876/`。真实 Safari 页面已请求向用户授权测试邮箱发送一次验证码，前端收到成功响应；截至最近检查 Auth 有 1 个未验证用户。API 成功不等于邮件已到达收件箱，等待用户自行输入验证码。
- 前端已接官方 Supabase JS：邮箱发送/验证、显式领取试用、账号额度列表、兑换、退出、过期清理和迟到请求防护。默认勾选“记住登录”时，会话保存在浏览器本地并在成功验证后续期，最长 7 天；未勾选时仅保留当前标签页。授权后一次取单小区新数据，合并旧历史并重算曲线，不改免费市场缓存。账号服务不可用也不阻断免费查询。
- UI 改为北京房价研究站：导航、搜索首页、市场研究卡片、账号弹窗、首次可关闭指南。指南可以从导航/页脚重开，不是强制注册遮罩。
- 管理员页面支持按邮箱签发兑换码、配置有效天数、兑换截止日期和最多查看次数，并可管理市场缓存元数据。运营工具 `scripts/manage_supabase_tokens.py` 仍可用于人工确认到账后的签发、幂等重试、列出元信息及撤销。明文码只在签发时展示，不进入公开数据；尚无自动支付和完整订单/退款系统。
- `tests/supabase_permissions.test.mjs` 用嵌入式 PostgreSQL (PGlite) + 虚构样本实际运行 SQL、角色/grants/RLS 与权限边界测试。不是字符串检查，但不能代替云端 Auth、PostgREST 和多连接竞争测试。
- `app.py` 默认不再签发访客试用 Cookie，不接受旧 cookie/token 作为正式权限。未登录可看所有小区的免费历史，不会因第三个小区而遮挡旧记录。旧 AccessStore 仅保留用于兼容测试，`main()` 不启用。
- 公开产物仍强制截至 2025-08-31。私有新数据、SMTP 密钥、兑换码不纳入 GitHub 或公开构建。

2026-09-23 自动验收：33 项 Python、40 项 UI、55 项权限/服务端、5 项 Pages 测试通过。完整 509,901 笔本地数据在默认 6 月、24 月同比、自定义基准和局部筛选四个场景，对完整 Edge 返回及四部分重组结果逐字段对照独立 SQL，全部通过。云端默认/24 月同比/自定义/筛选冷计算均通过，长窗口冷请求约 31 秒、缓存请求约 3 秒。真实 Auth 付费验证同请求并发仅扣一次、市场与小区共享余额、最后一次额度竞争和零余额合法重试通过；见 Actions 35816432230。公开快照保持 445,034 条、截至 2025-08-31。PGlite/JSDOM 仍不能替代真实网络、邮件和浏览器视觉验收，发布前必须继续执行下文检查。

### 补充验收记录（2026-09-17）

- `scripts/verify_supabase.sql` 在云端真实 PostgreSQL 上以 authenticated 角色验证：两个试用额度、重复不扣、第三个拒绝、分页日期边界、兑换/重复兑换不延长、跨账号拒绝、撤销生效、私有表不可直读。所有测试账号/兑换码均在单个事务内创建，结尾 ROLLBACK，已确认无残留；不等于 Auth REST 登录或多连接竞争测试。
- 云端与本机的新数据数量、面积合计、单价合计、总价合计、有效挂牌价笔数及合计一致；云端数据库当前约 25 MB。聚合对账不是逐行校验和。
- 使用 Safari + 390×844 CSS 像素 iframe 渲染本地 `docs/` 静态构建（未发布）：首次引导可关闭、返回不再弹出、小区搜索和详情正常，曲线/阴影及两列指标卡不挤出视口。这是手机宽度布局验证，不是实际 iOS 设备或触摸全流程测试。
- 高德安全密钥只存 Supabase Secrets；已部署受限 `amap-proxy` Edge Function。前端只取得公开 Web Key 与代理地址，代理限制来源、路径、参数和调用额度，不把安全密钥发送到浏览器。
- 购买窗口已展示微信手机号 `15611710247` 和备注“房价分析工具”。套餐价格、有效期、查看次数与售后仍由购买前人工确认。

## 每次发布顺序

1. 重新构建 `static/cloud.js`，运行 UI、权限/服务端、Pages 与 Python 全量测试。
2. 生成 `docs/`，执行公开发布保护；公开 SQLite、预计算结果和月分片都必须逐项证明最晚不超过 2025-08-31。
3. 审计 Git 跟踪文件，确认没有付费 CSV/SQLite、service role、SMTP 密钥、高德安全密钥或真实兑换码。
4. 在桌面与 390px 视口完成首页、筛选、排行、小区曲线、地图、账号弹窗、最新报告入口和管理员锁定态的浏览器回归。
5. 提交公开代码后，将 `MARKET_PUBLIC_BASE` 固定为该 40 位 commit 的 `raw.githubusercontent.com/.../docs/` 地址，依次部署 `market-compute` 与 `market-report`。公开分片未变化时可保持原固定 commit。函数只接受 manifest 中的固定文件并核验大小与 SHA-256。
6. 用真实已验证账号检查管理员自动最新报告、付费显式扣次、失败不扣次和同一请求重试不重复扣次；最后验证 GitHub Actions、正式 URL 与控制台。

## 产品规则

1. **未登录用户**：不强制注册、不创建匿名账号，可搜索并查看所有小区截至 2025-08-31 的历史明细、成交/挂牌曲线、成交量及旧数据的区域统计。查看旧数据永不消耗试用额度。
2. **已验证邮箱的注册用户**：仍可查看全部旧数据；累计选择 2 个不同小区，免费解锁这两个小区从 2025-09-01 起至数据末尾的全部已收录数据（包括 2026 年）。同小区重复查看不扣额度，刷新、换设备或再次登录也不重置。两个名额不是每次登录两个，不默认按月重置。
3. **付费用户**：邮箱登录并兑换有效 token 后，可查看更多小区的新数据，也可显式查询最新完整市场报告。每次成功查看一份报告或一个非试用小区各扣 1 次，共用余额；同一请求的短时网络重试不重复扣次。
4. 全市、区级统计和热力图免费截至 2025-08-31；注册赠送两个小区的试用不等于解锁全市最新排行榜或最新热力图。
5. “看 2026 年数据”是注册引导文案，不另外把 2025-09 至 2025-12 遗漏或公开；保留此前确认的 2025-08-31 / 2025-09-01 边界。若用户要改成仅 2026 年收费，需要另行明确修改。
6. 搜索默认只展示小区目录和免费日期范围内统计，不泄露最新逐笔数据。受限 UI 不接收真实值后再 CSS 模糊，不能用注册前下载完整库的方式实现。
7. 用户首次点击“解锁该小区新数据”时提示“将使用 1 个名额，解锁后可重复查看”；只有用户确认且小区确有新数据时才占用额度。查旧记录、仅搜索、无新数据或请求失败不扣额度；如果额度登记成功但网络响应丢失，重试应返回原解锁状态，不二次扣减。
8. 超过两个小区时，只锁定该小区的新数据，旧数据仍然可以查看。退出登录或付费到期后，回到未登录/注册用户应有的权益，不把全部旧历史一并锁住；已选试用小区按账号保留。

## 页面路径

免费打开 → 搜索任意小区看旧历史 → 点击“查看新成交（含 2026 年）” → 邮箱验证码注册/登录 → 明确选择 1 个试用小区 → 展示最新历史。

两个名额用完后，再解锁其他小区 → 展示“联系购买”与“已有兑换码”两个入口；不能只给一个没有购买说明的 token 输入框。

未登录时的注册提示仅在访问新数据时出现，不作为进站遮罩或查看旧数据的前置条件。首次验证邮箱完成注册，已有邮箱走同一登录入口。

## 第一版人工售卖闭环（当前方式）

采用“网站购买说明 + 微信私聊人工确认收款 + 独立兑换码”，微信群可作为可选交流渠道，不强制进群。网站只展示已确认的手机号和备注，不展示未经确认的价格或套餐承诺。

1. 网站说明可查看哪些数据、截止日期、套餐有效期、价格、交付时效、售后/退款规则、限制与免责声明；展示用户提供的客服微信/企业微信二维码及可复制联系方式。
2. 用户联系购买，提供站内账号编号和套餐。不要要求用户在群中公开邮箱、订单信息或兑换码，也不索取邮箱验证码或密码。
3. 运营方确认实际到账，记录订单编号、账号编号、金额、套餐和状态；付款截图不能单独作为到账依据，不能只凭用户点“已付款”发码。
4. 每笔订单生成唯一随机 token，私聊交付。签发/发码重试必须关联同一订单，避免重复授予权益；无须把一批明文 token 发布到群里。
5. 用户在网站登录并兑换；推荐兑换时绑定已验证账号，若订单指定了账号则必须匹配。token 为兑换码，而非多人可分享的永久登录密码。
6. 推荐权益从首次兑换成功时起算，兑换码本身另设兑换截止期；重新输入相同 token 不重置有效期、不重复延长。定价、时长、续费叠加方式和售后规则需单独确认。
7. 同一账号已兑换再次请求时返回既有权益；其他账号不能重复兑换。退款或违规处置可撤销权益并记录原因，下一次受保护请求立即按新权限判断。
8. 后期订单量增加，再接合适的正规支付提供商；由服务端验签后的支付回调发放权益，验证金额/订单、去重并处理退款事件。Supabase 负责权限与订单状态，不自身代替收款渠道。

不推荐把微信群当成唯一购买入口：用户需额外进群，二维码要维护，群消息易淹没订单，公开发码有串号/转发风险。群更适合产品交流、更新通知和客服答疑。人工售卖也要明确数据使用授权、个人信息处理和售后规则，不等于没有经营或平台要求；这里没有做特定平台合规结论。

## 部署结构

浏览器 / GitHub Pages → Supabase Auth、`amap-proxy` 与 `market-report` Edge Functions → 受控 PostgreSQL RPC → 非公开数据表与私有计算缓存。

GitHub 保存前端和迁移代码，不保存付费 CSV、SQLite、token 明文或管理员密钥。

- Auth：使用邮箱 OTP，验证成功才领取额度，按服务端核验的 `auth.uid()` 记录。未登录访问旧数据不调用匿名注册、不索取邮箱。不能接受客户端传来的任意邮箱或 user_id 作为已验证身份。邮箱也不能杜绝多账号，配合验证码、邮件发送限速和异常检测。
- `housing_private.transactions`：本轮迁移改为只存 2025-09-01 起的新成交，旧数据沿用公开快照；使用日期和小区复合索引。非公开 schema，不向 anon/authenticated 授予直接 SELECT，不加入 Realtime 发布。开启 RLS 作为纵深防御。
- `community_catalog`：只公开小区标识、区、商圈等目录信息，不附最新成交统计。
- `trial_unlocks`：按 `(user_id, community_id)` 唯一，锁定该用户额度行后再计数/插入；同事务判断，避免并发开第 3 个。旧浏览器访客 Cookie 不能直接领取/迁移新账号额度。
- `orders`（待实现）：完整金额/收款/退款流水；目前仅 `paid_tokens.order_ref` 唯一编号和本机发码记录，不是财务订单系统。
- `paid_tokens`：高熵随机 token 的哈希、兑换截止期、权益时长、订单关联、撤销状态、首次绑定账号和兑换时间；不可由用户直接更新。原本地 token 仅登录会话的行为不能直接视为此兑换模型。
- 权益由 `paid_tokens` 的账号/有效期/撤销状态推导，每次受保护查询都重新核对，不仅信任 JWT；当前无单独 `entitlements` 表。
- 查询：旧数据允许未登录查询；新数据先验证 Auth 用户，再验证日期/小区试用或付费权益。只返回授权对象和所需字段，不能因注册后一次通过而允许查询任意小区的新数据。交易明细分页且限制单次行数，不下发全量私有数据库。
- 统计：免费市场仍用公开预计算结果；最新完整报告由 `market-report` 临时读取固定 commit 下经过 manifest/SHA-256 校验的所需免费月分片，在 Edge 中与受控分页的私有增量精确计算。滑动中位数、P30/P60、最高/最低、样本门槛、排行和热力图均沿用同一口径。免费逐笔历史不持久写入 Supabase。
- 缓存：免费结果与付费结果隔离；最新报告只进入私有缓存，受控 TTL、总预算、数据修订号和管理员清理。受保护响应 `no-store`；退出、撤权、过期或切回免费模式会清除最新图表和客户端状态。
- 地图：高德安全密钥放服务端 secret，`amap-proxy` 只开放所需路径/参数并限速；公开 JS Key 通过服务端配置接口下发并配域名白名单。小区坐标在客户端与共享位置缓存中复用，避免每次筛选重复地理编码。

## 安全边界

- Supabase publishable key / 旧 anon key 可出现在前端，但安全性由 grants、RLS、函数权限和鉴权保证。`service_role` / secret key / 数据库密码绝不能放 GitHub Pages。
- 使用 SECURITY DEFINER RPC 时固定 search_path、限定 EXECUTE 授权，显式校验 auth.uid()。RLS 不自动约束此类函数，必须单独测试。
- 只允许已审核接口读取私有 schema；不开放通用 SQL、任意表查询、全量导出或 Storage 公共桶。
- HTTPS、服务商存储保护及可选私有加密备份用于保护传输/静态存储；不能靠把解密 key 放前端防下载。
- 用户有权看到的 2 个小区/付费结果仍可能被保存、截图。分页、限速、水印、审计只能增加批量搬运成本，无法实现绝对防复制。
- 历史上已经公开的免费数据及 Git 历史不能靠新加 token 收回；若要限制全部旧数据的小区访问，需要移除静态数据库并审计历史，但此前下载的副本不可回收。

## 上线前需要

1. Supabase 项目 ref `mehbviiakjcbfckonzqk` 已授权并导入，地域经用户确认；上线前确认数据库容量和流量预算。
2. 邮箱 OTP 发送配置、邮件模板、登录回跳地址及验证码/限速。新策略已不采用匿名试用；邮箱验证要做真实邮件收发验收。
3. 项目授权连接方式。私钥放本机忽略文件/环境变量或平台 secrets，不在聊天或源码中公开。不要为方便测试把 service_role key 配进浏览器。
4. 原始数据对外提供服务的授权范围仍需运营方核实；云端仅导入 64,867 条新增数据，另有索引、Auth、审计和受预算约束的报告缓存，不能只以源 SQLite 体积估算总容量。
5. 用户受众主要在中国大陆，上线前实测 Supabase API、登录邮件、地图代理的成功率和延迟；不要未经测试承诺连通性。
6. 已选择微信私聊人工交付；正式收款前仍需由运营方明确套餐价格、时长/次数、交付时效、退款与售后规则，并记录真实订单。

## 验收

- 未登录可查看所有小区截至 2025-08-31 的全部有效旧记录，访问 2025-09-01 起数据要求登录或权益；未验证邮箱不能领取额度，旧浏览器 Cookie 不授予新数据访问。
- 注册账号选中的第 1、2 个小区最新历史可见，第 3 个的新数据不可见、旧数据仍可见；重复/并发调用不多扣、不超额。跨设备同账号保持额度，无新数据和失败请求不占新额度。
- 直接请求 REST 表、RPC、伪造 user_id、日期、分页游标、换小区参数均不能扩大权限。
- token 无效/到期/撤销/跨账号重复兑换；多标签页退出及旧请求迟到不残留付费内容。
- 人工订单确认、重复发码、重复兑换、支付未确认不发码、退款撤权；从首次兑换计时，重复兑换不延长；令牌/订单/账号不能被普通用户批量枚举。
- 新旧后端对账单价、成交量、排行榜、地图与曲线；浏览器桌面/手机渲染和拖拽、hover 验证。
- 公开构建不包含私有数据/密钥，公开用户无法下载完整成交库；云端权限测试通过后才发布。

## 官方依据（本次已读取）

- https://supabase.com/docs/guides/api/securing-your-api ：grants 与 RLS 两层权限、非公开 schema、额度/限速前置校验、SECURITY DEFINER 注意事项。
- https://supabase.com/docs/guides/auth/auth-anonymous ：匿名身份清除浏览数据后无法恢复、验证码/限速防滥用建议。
